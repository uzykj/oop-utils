/**
 * @author ghostxbh
 * @version 1.0.0
 * @description OptionalConstant
 */
export default {
  MustBePresent: 'Optional item must be present.',
  AtLeastOnePresent: `Either the 'of' value or the 'or' value must be present.  Consider using Optional.orNull() / orUndefined() instead of Optional.or(null || undefined).`,
  MissingTransformFunction: 'Transform Function must be given.'
}
